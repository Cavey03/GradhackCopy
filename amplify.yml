version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 22
        - 'if [ -z "$EXPO_PUBLIC_API_URL" ]; then echo "Missing required Amplify environment variable: EXPO_PUBLIC_API_URL"; exit 1; fi'
        - npm ci
    build:
      commands:
        - npx expo export --platform web
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
